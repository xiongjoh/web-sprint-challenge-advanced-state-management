
const initialState = {
    isLoading: false;
    smurfs:[],
    error:'',
}

const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default reducer